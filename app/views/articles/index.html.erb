<h1> All of the articles mf <h1>

<table>
    <tr>
        <th> Title </th>
        <th> Text </th>
        <th></th>
    <tr>


<% @articles.each do |article| %>
    <tr>
        <td><%= article.title %></td>
        <td><%= article.text %></td>
        <td><%= link_to 'Show', article_path(article) %></td>
        <td><%= link_to 'Edit', edit_article_path(article)%>
        <td><%= link_to 'FUCKITUP', article_path(article),
                method: :delete,
                data: { confirm: "You gon' fuck this up, huh?" } %></td>
    </tr>
<% end %>
</table>